# frozen_string_literal: true

# NOTE: This Gemfile is eval'd into Redmine's own bundle context.
# Do NOT declare gems that Redmine already manages (e.g. rubocop, minitest).
# This file can be left empty or used only for gems truly unique to this plugin.
#
# To run the plugin tests, from your Redmine root:
#   bundle exec rails test plugins/redmine_quick_assign/test
